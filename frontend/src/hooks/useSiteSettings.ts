/** Re-export hook site settings (nguồn chính: lib/useSiteSettings.tsx). */
export {
  useSiteSettings,
  SiteSettingsProvider,
  getContactInfo,
  getBrandAssets,
  SITE_SETTINGS_DEFAULTS,
  type SiteSettings,
} from "../lib/useSiteSettings";
